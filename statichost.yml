# Hugo docker image
image: peaceiris/hugo:v0.122.0-mod
# Build command
command: hugo mod clean && hugo mod get github.com/jpanther/congo/v2@v2.8.0 && hugo --minify --destination build --environment production
# Build output directory to deploy
public: build
